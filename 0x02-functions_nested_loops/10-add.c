#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 * @i: number one.
 * @k: number two.
 * @Return: Add of number one and number two.
 */

int add(int i, int k)
{
	return (i + k);
}
